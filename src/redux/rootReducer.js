import {combineReducers} from 'redux';
import * as userReducer from './users/user.reducer'

const rootReducer = combineReducers({
    // userinfo:userReducer.userReducer

});

export default rootReducer;
